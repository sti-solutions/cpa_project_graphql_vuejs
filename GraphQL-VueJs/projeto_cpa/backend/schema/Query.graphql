# Pontos de entrada da sua API!
type Query {
    usuarios: [Usuario]
    usuario(filtro: UsuarioFiltro!): Usuario
    perfis: [Perfil]
    perfil(filtro: PerfilFiltro!): Perfil
    companys: [Company]
    company(filtro:CompanyFiltro!): Company
    courses: [Course]
    course(filtro:CourseFiltro!): Course

}